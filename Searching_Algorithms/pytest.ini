[pytest]
minversion = 6.0
addopts = -ra --cov=Searching_Algorithms --cov-report=term-missing
testpaths = 
    tests
pythonpath = .
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    smoke: quick sanity tests to verify basic functionality
    regression: tests for previously fixed bugs
